<p id="notice"><%= notice %></p>
   <div class="container">
	    <div class="row">
	        <div class="col-sm-6 col-md-4 col-md-offset-4">            
	            <div class="account-wall">
	                <img class="profile-img" src="<%= asset_path 'user.png' %>" alt="">
			      <%= form_for(:session, url: login_path, html: {class: "form-signin"}) do |f| %> 
			        <input type="text" class="form-control" placeholder="Email" name="email" required autofocus>
	                <input type="password" class="form-control" placeholder="Password" name="password_digest" required>
	                <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>	                
			      <% end %>
	            </div>
	            <a href="<%= new_user_path %>" class="text-center new-account">Crear una cuenta </a>
	        </div>
	    </div>
   </div>